#include <stdio.h>
#include <stdlib.h>

/**
 * main - Entry point
 *
 * Return: Always 0 (Success)
 */

int main(void)
{
char dex;
for (dex = 'z' ; dex >= 'a' ; dex--)
putchar(dex);
putchar('\n');
return (0);
}
